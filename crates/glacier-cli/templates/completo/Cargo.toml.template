[package]
name = "{{nome_projeto}}"
version = "0.1.0"
edition = "2024"

[dependencies]
# O motor. Ele reexporta o `iced` (`glacier_ui::iced`), então este é o único
# crate de UI que o projeto precisa declarar.
glacier-ui = "{{versao_motor}}"
